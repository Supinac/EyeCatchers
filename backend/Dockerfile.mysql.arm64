FROM --platform=linux/arm64 mysql:8.4

# Optional defaults; override at runtime with -e flags.
ENV MYSQL_DATABASE=eyecatchers
ENV MYSQL_USER=eyecatchers
ENV MYSQL_PASSWORD=eyecatchers
ENV MYSQL_ROOT_PASSWORD=root

EXPOSE 3306
